
--local function
function main()
	--local const variable
	myVar = 5
	--global variable
	GlobalVar <global,mutable> = 6
	--mutable local variable
	i <mutable> = 1
	--increase i by 1
	i += 1
	--decrease i by one
	i -= 1
	--multiply i by 2
	i *= 2
	--divide i by 2
	i /= 2
	-- all valid lua syntax is also valid dysnomia syntax
	local var1 <const> = add(1,2)
end

--global function
global function add(a,b)
	return a + b
end


i = 7

main()