record myRec(a,b,c,d) endRec

local function(hello,world)
	i + 1
	j - 1 --doing nothing
	k += 1
	--trying sub operator
	m -= 4
	"this next one will be multiply"
	n *= 6
end

--do stuff


local tbl = {}

v /= 7

"this is a string"

function("this is an arg") --this is a comment

